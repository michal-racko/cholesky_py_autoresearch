# Idea backlog

Maintained by the autoresearch agent. Keep Open ordered by expected payoff /
effort, and reference benchmark entries by their `task.yml` specs.

## Open

(empty — generate ideas from the current `submission.py`, `task.yml`, and the
ledger)

## Tried

(nothing yet — harness verdicts land in `ledger.jsonl`; record findings here)

## Dead ends

(none yet)